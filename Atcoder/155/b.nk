import "io";

fn main() -> i32 {
	n := input();
	a := i32 list;
	for(i in 0..n-1)
		a.add(input());

	for(i in 0..n-1)
		if(a[i] % 2 == 0 && a[i] % 3 != 0 && a[i] % 5 != 0) {
			writefln("DENIED");
			ret 0;
		}
	writefln("APPROVED");
	ret 0;
}