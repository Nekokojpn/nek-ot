fn rsa_encrypto(p : f64, q : f64){
	writefln("p=%lf", p);
	writefln("q=%lf", q);
	n := f64$ p*q;
	writefln("n=%lf", n);
	fi := f64$ (p-1) * (q-1);
	writefln("É”(n)=%lf", fi);

}


fn main(){
	writefln$"%d", abs(5);
	ret;
}