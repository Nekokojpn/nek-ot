import "io";
fn aryArg(arr: i32[]) -> i64 {
	ret arr.len();
}
fn main(){
	ary := i32[5]{1,2,3,4,5};
	i := i32 input();
	writefln("%d", aryArg(ary));
	writefln("%d %d %d %d", ary.len(), ary[0], ary[4], ary[i]);
	ret;
}
