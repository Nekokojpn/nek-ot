fn main(){
	MAX := i32$447;
	DN := i32$4;
	x := i64$0LL;
	y := i64$0LL;
	sx := i64$0LL;
	sy := i64$0LL;
	zx := i64$0LL;
	zy := i64$0LL;
	cx := i64$0LL;
	cy := i64$0LL;
	xx := i64$0LL;
	yy := i64$0LL;

	n := i32$0;
	sn := i32$0;
	h := i32$0;

	while(y < 384){
		x = 0LL;
		while(x < 512){
			sn = 0;
			sx = 0LL;
			while(sx < DN){
				cx = (x * 4 + sx) * 56 + 4673536LL;
				sy = 0LL;
				while(sy < DN){
					cy = (y * 4 + sy) * 56 + 124928LL;
					zx = 0LL;
					zy = 0LL;
					n = 0;
					while(n < MAX){
						xx = (zx * zx) >> 24;
						yy = (zy * zy) >> 24;
						if(xx + yy > 67108864)goto jmp;
						zy = (zx * zy) >> 23;
						zx = xx - yy + cx;
						zy = zy - cy;
						n = n + 1;
					}
					@jmp:
					sn = sn + n;
					sy = sy + 1LL;
				}
				sx = sx + 1;
			}
			n = sn >> 4;
			h = h + n;
			x = x + 1;
		}
		y = y + 1;
	}
	writefln("%d", h);
	ret;
}