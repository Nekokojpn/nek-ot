fn main(){
	i := i32$0;
	f := f32$0.0;
	i = i + 2; // 2
	writefln("%d", i);
	i = i - 1; // 1
	writefln("%d", i);
	i = i * 10; // 10
	writefln("%d", i);
	i = i / 10; // 1
	writefln("%d", i);
	//	0001
	//	0011
	// =0010
	i = i xor 3; // 2
	writefln("%d", i);
	i =	i >> 1; //0001 = 1
	writefln("%d", i);
	i = i << 1; //0010 = 2
	writefln("%d", i);
	ret;
}