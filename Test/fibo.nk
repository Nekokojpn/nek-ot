fn fibo(i32 n) -> i32{
val := i32(0);
 if(n < 2){
  val = n;
 }
 else {
   val = fibo(n-1) + fibo(n-2);
  }
  ret val;
}
fn main() {
i := i32(1); 
 while(i < 35){
 writefln("fibo(%d) -> %d", i, fibo(i));
 i = i + 1;
 }
 ret;
}