import "io";

fn main(){
	x := 0; y := 0; z := 0;
	if((x, y, z) == 0)
		writefln("OK!");
	else
		writefln("NG!.");
	x = 1; y = 1; z = 1;
	if((x, y, z) == 1)
		writefln("OK1!");
	else
		writefln("NG1!");
	ret;
}
