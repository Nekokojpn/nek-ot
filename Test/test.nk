import "io";

fn dis(arr : i32[3]){
	for(i := 0; i < 3; i = i+1;){
		writefln("%d", arr[i]);
	}
	if(arr[0] < arr[1]){
		arr[0] = arr[1];
	}
	ret;
}

fn main(){
	arr := i32[3]{1,2,3};
	dis(arr);
	ret;
}