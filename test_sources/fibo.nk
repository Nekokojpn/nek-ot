fn fibo(int n) -> int{
int val = 0;
 if(n < 2){
  val = n;
 }
 else {
   val = fibo(n-1) + fibo(n-2);
  }
  ret val;
}
fn main() -> int{
 int i = fibo(34);
 string d = "%d";
 writefln(d, i);
 ret i;
}